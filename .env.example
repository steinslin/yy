# 服务端口（默认 3000）
PORT=3000

# 生产环境必须设为 production，否则不会托管前端静态资源
NODE_ENV=production

# 数据库（本地 MySQL）
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=login_db

# 可选：跨域白名单，多个用逗号分隔；不设则允许所有来源
# CORS_ORIGIN=
